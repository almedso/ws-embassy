[workspace]
resolver = "3"
members = [
    "firmware-core",
    "firmware-sim",
    "firmware-stm32f401re"
]

[workspace.dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
